/*
import "jest";

// scss 인식못함, alias 인식 못함 => 무시, 설정
// 렌더링은 Router안에서 해야함 => ?? 다해야함..?
// 우당탕탕 나서 보통 테스트 안하나,,,,,,?
// 함수를 테스트하는건가봄,,?
// 테스트코드,, 너무 만만하게 봤나,,,,,
// 약간 열받는점,, 파일 벗어나면 혼자 맘대로 테스트해서 실패라고 빨간색 만든다ㅠ
describe("SearchBar.KeywordInput 컴포넌트 테스트", () => {
 it("일단 렌더링을 해본다"),
 	() => {
 		const { getByPlaceholderText } = render(
 			<SearchBar>
 				<SearchBar.KeywordInput />
 			</SearchBar>,
 		);
 		const inputElement = getByPlaceholderText("검색어를 입력해주세요");
 		expect(inputElement);
 	};
	it("일단 테스트 설정의 테스트를 한다"),
		() => {
			console.log("얍");
		};
});
*/
