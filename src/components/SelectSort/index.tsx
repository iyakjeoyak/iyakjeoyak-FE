import {
	SortCurrentOption,
	SortOption,
	SortOptionList,
	SortSelectRoot,
} from "./UI";

const SelectSort = Object.assign(SortSelectRoot, {
	SortOption,
	SortCurrentOption,
	SortOptionList,
});

export default SelectSort;
